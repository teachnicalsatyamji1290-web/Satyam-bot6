services:
  - type: web
    name: SATYAM-BOT
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node bot.js"
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
    autoDeploy: true
